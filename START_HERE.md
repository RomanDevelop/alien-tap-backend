# 🚀 Быстрый старт - Что делать дальше?

## ✅ Выполнено:

- ✅ База данных `alien_game` создана
- ✅ Миграции применены (таблицы users, scores, claims созданы)
- ✅ Файл `.env` создан (нужно добавить TELEGRAM_BOT_TOKEN)

## 📋 Следующие шаги:

### 1. Установите Rust (если еще не установлен)

Откройте терминал и выполните:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

После установки:

- Закройте и откройте терминал заново
- Или выполните: `source $HOME/.cargo/env`

Проверка:

```bash
cargo --version
```

### 2. Настройте .env файл

Откройте файл `.env` в корне проекта и добавьте:

**Telegram Bot Token:**

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Следуйте инструкциям и создайте бота
5. Скопируйте полученный токен
6. Вставьте в `.env` вместо `your_bot_token_here`

Пример:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 3. Соберите проект

```bash
cd "/Users/anymacstore/Flutter development/alien_tap_backend"
cargo build --release
```

**Примечание:** Первая сборка может занять 5-10 минут (скачивание зависимостей).

### 4. Запустите сервер

```bash
cargo run
```

Вы должны увидеть:

```
Starting server on port 8000
Connected to database
Server listening on http://0.0.0.0:8000
```

### 5. Проверьте работу

Откройте **новый терминал** и выполните:

```bash
curl http://localhost:8000/health
```

Должен вернуться: `{"status":"ok"}`

## 🎉 Готово!

Ваш backend запущен и готов к работе!

### Полезные ссылки:

- **Документация API:** `README.md`
- **Примеры интеграции с Flutter:** `INTEGRATION_EXAMPLE.md`
- **Быстрый старт:** `QUICKSTART.md`

### Команды для работы:

```bash
# Запуск сервера
cargo run

# Сборка проекта
cargo build --release

# Проверка кода
cargo check

# Проверка здоровья сервера
curl http://localhost:8000/health
```

### Остановка сервера:

Нажмите `Ctrl+C` в терминале где запущен `cargo run`
