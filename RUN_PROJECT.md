# 🚀 Запуск проекта после установки Rust

## Шаг 1: Загрузите переменные окружения

В вашем терминале выполните:

```bash
source "$HOME/.cargo/env"
```

**Или** просто закройте и откройте терминал заново (PATH загрузится автоматически).

## Шаг 2: Проверьте установку

```bash
cargo --version
```

Должна показать версию, например: `cargo 1.91.0`

## Шаг 3: Перейдите в проект

```bash
cd "/Users/anymacstore/Flutter development/alien_tap_backend"
```

## Шаг 4: Настройте .env файл

Откройте файл `.env` и добавьте токен бота:

1. **Получите токен у @BotFather:**

   - Откройте Telegram
   - Найдите [@BotFather](https://t.me/BotFather)
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

2. **Отредактируйте `.env`:**
   - Откройте файл `.env` в редакторе
   - Замените `your_bot_token_here` на реальный токен
   - Сохраните файл

Пример:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

## Шаг 5: Соберите проект

```bash
cargo build --release
```

**Важно:**

- Первая сборка займет **5-10 минут** (скачивание зависимостей)
- Последующие сборки будут быстрее

## Шаг 6: Запустите сервер

```bash
cargo run
```

Вы должны увидеть:

```
Starting server on port 8000
Connected to database
Server listening on http://0.0.0.0:8000
```

## Шаг 7: Проверьте работу

Откройте **новый терминал** и выполните:

```bash
curl http://localhost:8000/health
```

Должен вернуться: `{"status":"ok"}`

## 🎉 Готово!

Ваш backend запущен и готов к работе!

### Полезные команды:

```bash
# Остановить сервер
Ctrl+C (в терминале где запущен cargo run)

# Перезапустить после изменений
cargo run

# Проверить код без запуска
cargo check
```

### Что дальше:

1. Протестируйте API эндпоинты (см. `QUICKSTART.md`)
2. Интегрируйте с Flutter WebApp (см. `INTEGRATION_EXAMPLE.md`)
3. Начните разработку!
