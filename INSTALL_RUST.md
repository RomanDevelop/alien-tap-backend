# 🦀 Установка Rust

## Установка через rustup (рекомендуется)

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

После установки:

1. Закройте и откройте терминал заново
2. Или выполните: `source $HOME/.cargo/env`

## Проверка установки

```bash
rustc --version
cargo --version
```

## После установки Rust

1. Установите зависимости проекта:

   ```bash
   cargo build
   ```

2. Создайте `.env` файл (см. ниже)

3. Запустите проект:
   ```bash
   cargo run
   ```
