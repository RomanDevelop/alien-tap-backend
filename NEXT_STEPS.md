# ✅ Миграции выполнены! Следующие шаги

## Шаг 1: Проверка таблиц в pgAdmin

В левой панели pgAdmin:

1. Разверните: `alien_game` → `Schemas` → `public` → `Tables`
2. Вы должны увидеть 3 таблицы:
   - ✅ `users`
   - ✅ `scores`
   - ✅ `claims`

Если таблицы видны - всё готово! 🎉

## Шаг 2: Создание .env файла

Создайте файл `.env` в корне проекта:

```bash
cp env.example .env
```

Затем отредактируйте `.env` и заполните:

```env
# Database (для вашей локальной PostgreSQL)
DATABASE_URL=postgres://anymacstore@localhost:5432/alien_game

# Если у вашего пользователя есть пароль:
# DATABASE_URL=postgres://anymacstore:your_password@localhost:5432/alien_game

# Telegram Bot Token (получить у @BotFather)
# Перейдите на https://t.me/BotFather и создайте бота, затем скопируйте токен
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather

# JWT Secret (любой секретный ключ, минимум 32 символа для безопасности)
JWT_SECRET=supersecretkey12345678901234567890

# Server port
PORT=8000
```

**Важно:**

- Получите `TELEGRAM_BOT_TOKEN` у [@BotFather](https://t.me/BotFather)
  - Откройте Telegram
  - Найдите @BotFather
  - Отправьте `/newbot`
  - Следуйте инструкциям
  - Скопируйте полученный токен в `.env`

## Шаг 3: Сборка проекта

```bash
# Проверьте, что Rust установлен
cargo --version

# Соберите проект (первая сборка может занять несколько минут)
cargo build --release
```

## Шаг 4: Запуск сервера

```bash
cargo run
```

Вы должны увидеть:

```
Starting server on port 8000
Connected to database
Server listening on http://0.0.0.0:8000
```

## Шаг 5: Проверка работы

Откройте новый терминал и выполните:

```bash
# Health check
curl http://localhost:8000/health
```

Должен вернуться: `{"status":"ok"}`

## 🎯 Готово!

Ваш backend готов к работе!

### Что дальше:

1. Протестируйте API эндпоинты (см. `QUICKSTART.md`)
2. Интегрируйте с Flutter WebApp (см. `INTEGRATION_EXAMPLE.md`)
3. Начните разработку игрового функционала!

### Полезные команды:

```bash
# Проверить статус сервера
curl http://localhost:8000/health

# Посмотреть логи
# Логи выводятся в консоль где запущен cargo run

# Остановить сервер
# Нажмите Ctrl+C в терминале где запущен cargo run
```
