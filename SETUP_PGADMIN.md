# 📋 Пошаговая настройка через pgAdmin 4

## Шаг 1: Создание базы данных

1. В **pgAdmin 4** (который у вас открыт):
   - В левой панели найдите и разверните **"PostgreSQL 16"**
   - Правой кнопкой мыши на **"Databases"**
   - Выберите **"Create" → "Database..."**

2. В открывшемся окне:
   - **Database**: `alien_game`
   - **Owner**: оставьте по умолчанию (или выберите `anymacstore`)
   - Нажмите **"Save"**

## Шаг 2: Применение миграций

1. В левой панели разверните созданную базу **"alien_game"**
2. Правой кнопкой на **"alien_game"** → **"Query Tool"**
3. Откройте файл `migrations/20240101000001_create_tables.sql` из проекта
4. Скопируйте **весь SQL код** из файла
5. Вставьте в Query Tool (окно запроса)
6. Нажмите **Execute (F5)** или кнопку ▶️
7. Должно появиться сообщение об успешном выполнении

## Шаг 3: Настройка .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Database configuration (адаптируйте под ваши настройки)
# Если используете пользователя по умолчанию без пароля:
DATABASE_URL=postgres://anymacstore@localhost:5432/alien_game

# ИЛИ если требуется пароль:
# DATABASE_URL=postgres://anymacstore:your_password@localhost:5432/alien_game

# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# JWT Secret (используйте сильный секретный ключ)
JWT_SECRET=supersecret

# Server port
PORT=8000
```

**Важно:** 
- Если у вашего PostgreSQL пользователя есть пароль, добавьте его в `DATABASE_URL`
- Получите `TELEGRAM_BOT_TOKEN` у [@BotFather](https://t.me/BotFather)

## Шаг 4: Сборка и запуск

```bash
# Проверка, что Rust установлен
cargo --version

# Сборка проекта
cargo build --release

# Запуск сервера
cargo run
```

Сервер будет доступен на `http://localhost:8000`

## Шаг 5: Проверка работы

### Health Check
```bash
curl http://localhost:8000/health
```

Должен вернуть: `{"status":"ok"}`

### Проверка базы данных в pgAdmin

1. В pgAdmin разверните **"alien_game" → "Schemas" → "public" → "Tables"**
2. Вы должны увидеть 3 таблицы:
   - `users`
   - `scores`
   - `claims`

## 🐛 Решение проблем

### Ошибка подключения к БД

Если видите ошибку `connection refused` или `authentication failed`:

1. Убедитесь, что PostgreSQL запущен (в приложении PostgreSQL 16 должно быть "Running")
2. Проверьте правильность `DATABASE_URL` в `.env`
3. Попробуйте разные форматы:
   ```env
   # Без пароля
   DATABASE_URL=postgres://anymacstore@localhost:5432/alien_game
   
   # С паролем
   DATABASE_URL=postgres://anymacstore:password@localhost:5432/alien_game
   
   # С указанием host (если нужно)
   DATABASE_URL=postgres://anymacstore@127.0.0.1:5432/alien_game
   ```

### Ошибка "database does not exist"

Убедитесь, что база данных `alien_game` создана через pgAdmin (см. Шаг 1)

### Ошибка "relation does not exist"

Примените миграции через Query Tool в pgAdmin (см. Шаг 2)

## ✅ Готово!

После выполнения всех шагов ваш backend готов к работе!

Следующие шаги:
- Получите токен бота у @BotFather
- Добавьте его в `.env`
- Протестируйте API эндпоинты (см. `QUICKSTART.md`)

