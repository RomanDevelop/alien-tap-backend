# Инструкции для бэкенда — режим разработки (DEV_MODE)

## Что сделано

Добавлен режим разработки, который позволяет использовать мок-хэши Telegram для локальной разработки Flutter-приложения.

## Изменения в коде

1. **`src/config.rs`** — добавлено поле `dev_mode: bool` в `Config`

2. **`src/utils/telegram.rs`** — обновлена `verify_telegram_auth()` для пропуска мок-хэшей в dev-режиме

3. **`src/routes/auth.rs`** — передача `dev_mode` в функцию проверки

## Как использовать

### 1. Включить dev-режим

Добавьте в файл `.env`:

```bash
DEV_MODE=true
```

Или экспортируйте переменную:

```bash
export DEV_MODE=true
```

### 2. Перезапустить бэкенд

```bash
cargo run
```

## Как это работает

- В dev-режиме (`DEV_MODE=true`): хэши, начинающиеся с `mock_hash_for_development_`, автоматически пропускают проверку подписи Telegram

- В production (`DEV_MODE=false` или не установлен): требуется валидная подпись Telegram, мок-хэши отклоняются

## Тест авторизации с мок-хэшем

```bash
curl -X POST http://localhost:8000/auth/telegram \
  -H "Content-Type: application/json" \
  -d '{
    "hash": "mock_hash_for_development_test123456",
    "auth_date": "1730628000",
    "user": {
      "id": 1111,
      "username": "dev_user",
      "first_name": "Dev",
      "last_name": "User"
    }
  }'
```

Ожидаемый ответ:

```json
{
  "token": "eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...",
  "user_id": "uuid-здесь"
}
```

## Проверка работы

В логах бэкенда при авторизации с мок-хэшем должно появиться:

```
🔧 Dev mode: Allowing mock hash for development
```

## Важно

1. Не включайте `DEV_MODE=true` в production.

2. После изменения `DEV_MODE` в `.env` перезапустите бэкенд.

3. Используйте только для локальной разработки.

## Отключение dev-режима

Удалите `DEV_MODE=true` из `.env` или установите `DEV_MODE=false`, затем перезапустите бэкенд.
