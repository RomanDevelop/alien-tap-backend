# После установки Rust

## Шаг 1: Завершите установку

В терминале, где запущена установка Rust:

1. Нажмите **Enter** (стандартная установка)
2. Дождитесь завершения установки
3. Закройте и откройте терминал заново
   - Или выполните: `source $HOME/.cargo/env`

## Шаг 2: Проверьте установку

```bash
rustc --version
cargo --version
```

Должны увидеть версии, например:

```
rustc 1.81.0 (abc123...)
cargo 1.81.0 (def456...)
```

## Шаг 3: Перейдите в проект

```bash
cd "/Users/anymacstore/Flutter development/alien_tap_backend"
```

## Шаг 4: Настройте .env

Откройте файл `.env` и добавьте токен бота:

1. Получите токен у [@BotFather](https://t.me/BotFather):

   - Откройте Telegram
   - Найдите @BotFather
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен

2. Вставьте токен в `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_реальный_токен_здесь
   ```

## Шаг 5: Соберите проект

```bash
cargo build --release
```

**Примечание:** Первая сборка займет 5-10 минут (скачивание зависимостей).

## Шаг 6: Запустите сервер

```bash
cargo run
```

Вы должны увидеть:

```
Starting server on port 8000
Connected to database
Server listening on http://0.0.0.0:8000
```

## Шаг 7: Проверьте работу

Откройте **новый терминал** и выполните:

```bash
curl http://localhost:8000/health
```

Должен вернуться: `{"status":"ok"}`

## 🎉 Готово!

Ваш backend запущен и готов к работе!
